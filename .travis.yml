language: java
jdk:
  - openjdk11
branches:
  only:
    - main
before_install:
  - chmod +x gradlew

# travis ci서버의 home
cache:
  directories:
    - "$HOME/.m2/repository"
    - "$HOME/.gradle"
echo "디렉토리스"

script:
  - "./gradlew clean build"
  echo "빌드 클린업"

# ci 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - iamgood147@gmail.com
